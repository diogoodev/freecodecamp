// implementar um verificador simples de palindrome
const strNormal = "ola"
    let strArr = [];
    for(let i=0; i < strNormal.length - 1; i++){
      strArr = strNormal.slice('')
    }
    strArr.reverse();
    console.log(strArr)